<!DOCTYPE html>
<html>
<head>
    <title>Volunteers' Schedule</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            color: #333;
        }

        h1 {
            color: #0056b3;
            text-shadow: 1px 1px 2px #b0c4de;
        }

        p {
            font-size: 16px;
            color: #666;
        }

        table {
            width: 100%;
            margin-top: 20px;
            border-collapse: collapse;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }

        th, td {
            text-align: left;
            padding: 12px;
            border-bottom: 1px solid #ddd;
        }

        th {
            background-color: #007bff;
            color: white;
            border-top-left-radius: 6px;
            border-top-right-radius: 6px;
        }

        tr:nth-child(even) {
            background-color: #f2f2f2;
        }

        tr:hover {
            background-color: #ddd;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <h1>Volunteers' Upcoming Hours</h1>
    <table>
      <tr>
        <th>Volunteer Name</th>
        <th>Role</th>
        <th>Day of Week</th>
      </tr>
      <% @volunteer_assignments.each do |assignment| %>
        <tr>
          <td><%= assignment.volunteer.volunteer_name %></td>
          <td><%= assignment.role.role_name %></td>
          <td><%= assignment.time_slot.day_of_week %></td>
        </tr>
      <% end %>
    </table>
</body>
</html>
